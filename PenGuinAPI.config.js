module.exports = {
    enabled: true,   // The plugin will be enabled if this is set to true
    serverID: null,  // A server ID that can be selected by the user
    channelID: null, // The ID of the channel on which messages are to be sent
    otherSettings: {
        someFeatureEnabled: false
    }
};
